# Set the default terminal to support 256 colors
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"

# Set prefix key to Ctrl-A
set -g prefix C-a
unbind C-b
bind-key C-a send-prefix

# Split window bindings
unbind %
bind | split-window -h

unbind '"'
bind - split-window -v

# Reload config binding
unbind r
bind r source-file ~/.tmux.conf

# Pane resizing bindings
bind -r j resize-pane -D 5
bind -r k resize-pane -U 5
bind -r l resize-pane -R 5
bind -r h resize-pane -L 5
bind -r m resize-pane -Z

# Enable mouse support
set -g mouse on

# Set escape time to 0
set -sg escape-time 0

# Use vi keys in copy mode
set-window-option -g mode-keys vi

# Copy mode bindings
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-selection
unbind -T copy-mode-vi MouseDragEnd1Pane

# Plugin settings
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'jimeh/tmux-themepack'
# set -g @plugin 'catppuccin/tmux'

# # Override theme options for transparency
# set -g @powerline-status-bg "default"
# set -g @powerline-status-fg "white"
# set -g @powerline-status-left-area-left-bg "default"
# set -g @powerline-status-left-area-left-fg "white"
# set -g @powerline-status-left-area-middle-bg "default"
# set -g @powerline-status-left-area-middle-fg "white"
# set -g @powerline-status-left-area-right-bg "default"
# set -g @powerline-status-left-area-right-fg "white"
# set -g @powerline-status-right-area-left-bg "default"
# set -g @powerline-status-right-area-left-fg "white"
# set -g @powerline-status-right-area-middle-bg "default"
# set -g @powerline-status-right-area-middle-fg "white"
# set -g @powerline-status-right-area-right-bg "default"
# set -g @powerline-status-right-area-right-fg "white"
#
# # Set additional styles for transparency
# set-option -g status-style bg=default,fg=white
# set-option -g pane-border-style bg=default,fg=white
# set-option -g pane-active-border-style bg=default,fg=brightwhite
# set-option -g message-style bg=default,fg=white
# set-option -g mode-style bg=default,fg=brightwhite

set -g @themepack 'powerline/block/yellow'
# set -g @themepack 'basic'
# set -g @catppuccin_flavor 'mocha'
#
# set -g @catppuccin_window_right_separator "█ "
# set -g @catppuccin_window_number_position "right"
# set -g @catppuccin_window_middle_separator " | "
#
# set -g @catppuccin_window_default_fill "none"
#
# set -g @catppuccin_window_current_fill "all"
#
# set -g @catppuccin_status_modules_right "application session user host date_time"
# set -g @catppuccin_status_left_separator "█"
# set -g @catppuccin_status_right_separator "█"
#
# set -g @catppuccin_date_time_text "%Y-%m-%d %H:%M:%S"

# Always at eof
run '~/.tmux/plugins/tpm/tpm'
